<Window x:Class="TechnologistWpf.Views.OrderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Производственный заказ" Height="300" Width="400"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Номер заказа" />
            <TextBox x:Name="TxtOrderNumber" Margin="0,0,0,10" />
            <TextBlock Text="Рецепт (ID)" />
            <TextBox x:Name="TxtRecipeId" Margin="0,0,0,10" />
            <TextBlock Text="Планируемое количество (кг)" />
            <TextBox x:Name="TxtQuantity" Margin="0,0,0,10" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Сохранить" Click="BtnSave_Click" Width="80" />
                <Button Content="Отмена" Click="BtnCancel_Click" Width="80" Margin="10,0,0,0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>