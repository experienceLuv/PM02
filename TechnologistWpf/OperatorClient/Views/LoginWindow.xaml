<Window x:Class="OperatorClient.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Grid>
        <Border Background="#FFFBF7" CornerRadius="20" BorderBrush="#F5E1D2" BorderThickness="1">
            <StackPanel Margin="30" VerticalAlignment="Center">
                <TextBlock Text="🏭" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Аппаратчик" FontSize="22" FontWeight="Light"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <TextBox x:Name="TxtLogin" Margin="0,0,0,10" Padding="10" FontSize="14"/>
                <PasswordBox x:Name="TxtPassword" Margin="0,0,0,20" Padding="10" FontSize="14"/>
                <Button Content="Войти" Height="40" Background="#E6A8A3" Foreground="White"
                        FontWeight="SemiBold" BorderThickness="0" Click="BtnLogin_Click"/>
                <TextBlock x:Name="LblError" Foreground="#D32F2F" Margin="0,10,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>